<form method="post" action="{{ path('app_distance_delete', {'id': distance.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ distance.id) }}">
    <button class="btn">Delete</button>
</form>
