{% extends 'base.html.twig' %}

{% block title %}ModeTransport{% endblock %}

{% block body %}
    <h1>ModeTransport</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mode_transport.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ mode_transport.titre }}</td>
            </tr>
            <tr>
                <th>Impact_carbone</th>
                <td>{{ mode_transport.impactCarbone }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_mode_transport_index') }}">back to list</a>

    <a href="{{ path('app_mode_transport_edit', {'id': mode_transport.id}) }}">edit</a>

    {{ include('mode_transport/_delete_form.html.twig') }}
{% endblock %}
